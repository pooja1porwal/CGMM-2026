services:
  - type: web
    name: mp-darshan-360-api
    env: node
    plan: free
    rootDir: apps/backend
    buildCommand: npm install && npx prisma generate --schema prisma/schema.postgres.prisma && npx prisma db push --schema prisma/schema.postgres.prisma && npm run db:seed
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_URL
        value: https://mp-darshan-360.vercel.app
