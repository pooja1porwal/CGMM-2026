# Original vector assets

Logo artwork is defined as original SVG geometry in `src/generator/iconLibrary.ts`, rather than loaded from external images. The application favicon is `public/favicon.svg`.

Add future locally bundled artwork or properly licensed fonts here. Keep runtime generation independent of network services.
